Владелец автомобиля: номер машины, марка,
сведения о владельце, сведения об угоне
(логического типа).

Цепное хеширование

struct car
{
    char licensePlate[30]; //номер машины + КЛЮЧ /code
    char brand[30]; //марка /name
    char owner[100]; //сведения о владельце /faculty
    char stolen[1]; //сведения об угоне (логического типа) /новое
    car() {} /vuz
};

!!!В .cpp файлах будут комментарии к функцяиям читаай если непонятно!!!